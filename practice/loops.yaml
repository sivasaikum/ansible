- name: loops
  hosts: web
  become: yes
  tasks:
  - name: Installing the pacakages
    ansible.builtin.package:
      name: "{{ item }}"
      state: present
    loop:
    - gcc
    - mysql
    - nginx
    - git
    - zip
  